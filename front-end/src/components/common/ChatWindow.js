import { Modal } from "react-bootstrap";
import { useState } from "react";

function ChatWindow() {
//   const [show, setShow] = useState(true);
//   console.log(friend);


  return (
    <Modal show={true} style={{width: "100%", height:"80%", backgroundColor:"White"}} centered >
    </Modal>

  );
}

export default ChatWindow;
